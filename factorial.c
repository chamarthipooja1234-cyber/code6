#include<stdio.h>
int main()
{
int n,i;
long long fac=1;
factorial values
printf("enter a number:");
scanf("%d",&n);
if(n<0)
{
printf("factorial of negative numbers is not defined.\n");
}
else{
for(i=1;i<=n;i++)
{
fact=fact*i;
}
printf(factorial of %d is %||d\n",fact);
}
return 0;
} 